{
  "name": "library-manager",
  "version": "0.0.0",
  "description": "A simple library manager app that add, updates and deletes books",
  "main": "index.js",
  "scripts": {
    "watch": "onchange -i -k app.js -- node app.js",
    "start:developer": "start http://localhost:3000/ && npm run watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Braga",
  "license": "MIT",
  "devDependencies": {
    "csso": "^3.5.1",
    "npm-run-all": "^4.1.5",
    "onchange": "^5.2.0",
    "reload": "^2.4.0"
  }
}
